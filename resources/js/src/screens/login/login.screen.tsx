import React from "react";
import { usePageTitle } from "../../hooks/usePageTitle.hook";

const LoginPage = () => {
    usePageTitle();

    return (
        <div>
            <h1>Login Page</h1>
        </div>
    );
};

export default LoginPage;
